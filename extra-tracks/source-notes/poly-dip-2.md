# Poly Dip 2

The user supplied this track code on 2026-09-24. PolyTrack decodes its embedded name as `Poly Dip 2` and creator as `Waffles`; its native track ID is `586fbb2ef6e638f8d22e050342896497f22da6302ff081e434aa17bf6f75cf87`.

The track contains 87,098 parts. That exceeds the replay verifier's 20,000-part safety limit. It is available only as a local, unranked Extra Track challenge. Its runs are not written to Firestore or sent to the verification queue. The statement that nobody has finished it has not been independently verified.

The preview was generated with the game's native `TrackData.createThumbnail()` function, not copied from another source.
